#include "pch.h"

cpu_thread::cpu_thread()
{
	m_callback = nullptr;
	m_hThread = nullptr;
	m_idThread = 0;
	m_idThreadParent = 0;
	m_quitAsap = false;
}

cpu_thread::~cpu_thread()
{
	assert( IsRunning()==false );

	if ( m_idThread && m_hThread )
		TerminateThread(m_hThread, 0);

	if ( m_hThread )
		CloseHandle(m_hThread);
}



bool cpu_thread::Run()
{
	assert( IsRunning()==false );

	ui32 idThread;
	ui64 hThread = _beginthreadex(nullptr, 0, &cpu_thread::ThreadProc, (void*)this, CREATE_SUSPENDED, &idThread);
	if ( hThread==0 )
		return false;

	m_idThreadParent = GetCurrentThreadId();
	m_hThread = (HANDLE)hThread;
	m_idThread = idThread;
	ResumeThread(m_hThread);
	return true;
}

bool cpu_thread::Run(const _METHOD& callback)
{
	m_callback = callback;
	return Run();
}

void cpu_thread::Wait()
{
	if ( m_hThread )
	{
		WaitForSingleObject(m_hThread, INFINITE);
		CloseHandle(m_hThread);
		m_hThread = nullptr;
		m_idThread = 0;
		m_idThreadParent = 0;
	}
}

void cpu_thread::QuitAsap()
{
	m_quitAsap = true;
}



ui32 WINAPI cpu_thread::ThreadProc(void* pParam)
{
	cpu_thread* pThread = (cpu_thread*)pParam;

	pThread->m_quitAsap = false;
	if ( pThread->m_callback!=nullptr )
		pThread->m_callback();
	else
		pThread->OnCallback();

	pThread->m_idThread = 0;
	return 0;
}
